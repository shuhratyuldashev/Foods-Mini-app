import { Card, CardHeader, CardTitle } from "../../../shared/ui/card";

const CustomSelectWidget = () => {
  return (
    <Card>
      <CardHeader className="items-center">
        <CardTitle>Мы еше работаем над этой фичей...</CardTitle>
      </CardHeader>
    </Card>
  );
};

export default CustomSelectWidget;
