import TranslaterWidget from "../../widget/TranslaterWidget";

const HomePage = () => {
  return (
    <main className="h-[100vh] w-full flex items-center justify-center px-4">
      <TranslaterWidget />
    </main>
  );
};

export default HomePage;
